# iCost Analyzer

iCost Analyzer 是一个开源、第三方、非官方的 iCost 账单分析工具。把
iCost 导出的 `.xlsx` 或 `.xls` 账单拖进页面，就可以在浏览器里查看收入、
支出、分类、标签、币种、趋势、热力图和交易明细。

> 账单文件只在当前浏览器中解析，不会上传到服务器。本项目与 iCost 官方无
> 关联，也不会联网获取实时汇率。

## 适合谁

- 已经使用 iCost 记账，想在电脑上快速回顾账单的人。
- 想看清年度、月度、旅行或多币种消费结构的人。
- 需要按分类、标签、币种、关键词筛选交易明细的人。
- 关心隐私，希望账单数据留在自己设备上的人。

## 主要功能

- 导入 iCost 导出的 Excel 账单，支持 `.xlsx` 和 `.xls`。
- 在本地浏览器中解析第一个工作表，不需要账号或后端服务。
- 查看总支出、总收入、净结余、月均支出、日均支出、笔均支出、最大单笔和
  退款/报销金额。
- 使用月度趋势、分类占比、支出排行、币种分布、星期分布、标签排行、每日
  支出热力图和每日收支截面理解消费变化。
- 按时间范围、年份、日期区间、交易类型、币种、分类、标签和关键词筛选。
- 点击图表中的月份、分类或标签后，联动筛选其他图表和明细。
- 手动维护外币汇率，所有金额统一折算成人民币展示。
- 支持浅色、深色和跟随系统主题；焦点不在输入框时可按 `d` 切换主题。

## 快速使用

1. 在 iCost 中导出 Excel 格式账单。
2. 打开 iCost Analyzer 页面。
3. 将 `.xlsx` 或 `.xls` 文件拖到页面任意位置，或点击上传区域选择文件。
4. 等待浏览器完成解析，文件越大可能需要更久。
5. 查看指标、图表和交易明细，按需要调整筛选条件。
6. 如果账单里包含外币，在“汇率”区域检查或修改折算汇率。

导入后的账单数据只保存在当前页面内存中。刷新页面、关闭页面或替换文件后，
需要重新导入账单。

## 隐私说明

- 账单文件在浏览器本地解析，不会上传到服务器。
- 项目目前没有后端服务、远程解析、账号系统或遥测逻辑。
- 除主题偏好外，应用不会把账单数据写入浏览器存储。
- 请不要把真实账单文件、私人截图或包含个人信息的示例提交到公开仓库。

## 许可证

当前仓库尚未包含 `LICENSE` 文件。正式公开分发前，请补充适合项目的开源许可证。
